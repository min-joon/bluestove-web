---
// Backward-compatible English URL. Render the same landing page directly rather
// than using Astro's static redirect page, which can display redirect copy on
// some static hosts.
import Base from '../../layouts/Base.astro';
import BluestoveLanding from '../../components/BluestoveLanding.astro';
---

<Base>
  <BluestoveLanding />
</Base>
